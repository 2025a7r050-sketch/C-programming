#include <stdio.h>
int main(){
    int x , y;
    printf("Enter the value of x  and y : ");
    scanf("%d%d", &x , &y);
    (x>y)?printf("x is the largest") : printf("y is the largest");

    return 0;

}
